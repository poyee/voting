export class AppConstants {
  static BASE_PATH = '/api';
  static POLL_URI = `${AppConstants.BASE_PATH}/poll`;
  static VOTE_URI = `${AppConstants.BASE_PATH}/vote`;
  static AUTH_URI = `${AppConstants.BASE_PATH}/auth`;
}
